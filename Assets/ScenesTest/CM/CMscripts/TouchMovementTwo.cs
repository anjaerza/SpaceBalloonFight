using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TouchMovementTwo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GetComponent<Rigidbody>().useGravity = false;
    }

    // Update is called once per frame
    void Update()
    {
        //GetComponent<Rigidbody>().MovePosition(GameObject.FindGameObjectWithTag("Player").transform.position);
    }
}
